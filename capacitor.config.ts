import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'fr.samhain.pos',
  appName: 'Samhain POS',
  webDir: 'dist',
  android: {
    backgroundColor: '#111827',
  },
}

export default config
